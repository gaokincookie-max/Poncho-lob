# POCHO LAB v0.5

- レイヤー別 STICK/POP チェック状態を localStorage に保存。
- 通常シミュレーションと観察モードが同じレイヤー設定を参照。
- レイヤー変更は実行中なら次の接触から即反映。終了後の既存集計は再実行が必要であることを明示。
- 危険条件TOP30に「個別OFF / レイヤーOFF / DB OFF / 未実装」を区別表示。
- 裏成立率はOFF条件も意図的に計測する仕様をUI上で明示。
- include-disabled 設定も保存。
- キャッシュ回避のため v05 ファイル名へ更新。
