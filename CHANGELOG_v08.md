# POCHO LAB v0.8

本家ぽちょ v0.2.1 との同期を優先した修正版。

## 本家同期
- 初期ぽちょ7体を維持。
- 1射=3体セットへ変更。統計上の shot は3体まとめて1カウント。
- 同じ射出セット3体は解放後450ms、互いの挙動判定を無視。
- 上向き重力を本家と同じ `0.00135` へ変更。
- 本家v0.2.1の挙動条件DBをそのまま使用（active 157 / disabled 13 / unimplemented 8）。
- レイヤーの初期一括OFFを廃止。DB状態を基準に判定。
- 自動シミュレーションの1射あたり物理進行を約0.9秒へ延長し、長時間系履歴が育ちやすいよう調整。

## 観察モード
- 人間操作も3体セット化。
- 射出ゾーン内で3体の好きな1体を掴み、柔らかい三角ジョイントごと振り回して離す方式。
- AIも同じ3体セットを射出。
- 射出前3体は盤面密度・周囲判定・壁履歴の集計対象外。

## 役判定修正
- 「過去にN回接触」は現在の接触を含めないよう、接触直前スナップショットで判定。
- 「初対面/初接触」も接触直前の履歴で判定。
- `新入り` 用の `newlyJoined` / 接着前グループ情報を生成。
- `芋づる式` の「1秒以内」「1回の接触を起点」「3体以上が順番に接着」を、直近接着イベントと同一グループの連鎖で判定。
- 「第三者から押された」は、単なる直前接触ではなく、一定以上の速度差がある衝突によるpush履歴を1200msだけ有効とするよう厳格化。

## 注意
- 役条件そのものの難易度調整は行っていない。今回は実装・同期バグの修正のみ。
